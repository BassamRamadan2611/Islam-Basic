import { SearchQuraanAudioPipe } from './search-quraan-audio.pipe';

describe('SearchQuraanAudioPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchQuraanAudioPipe();
    expect(pipe).toBeTruthy();
  });
});
